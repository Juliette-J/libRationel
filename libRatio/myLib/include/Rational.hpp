#include <iostream>
#include <cmath>
#include <cstdlib>

#ifdef RATIONNELS_HPP
#define RATIONNELS_HPP

class Rationnel {
    public :
        Rationnel(const double &a, const double &b);
        Rationnel(const Rationnel &r);

        ~Rationnel() = default;

    private :

}

#endif